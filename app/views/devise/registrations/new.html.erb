<% content_for :title, t("auth.page_title.sign_up") %>
<% content_for :head do %>
  <%= stylesheet_link_tag "pages/auth" %>
<% end %>

<div class="auth">
  <div class="auth__card">
    <div class="auth__brand">
      <div class="auth__prism" aria-hidden="true">
        <span class="auth__ray auth__ray--soft"></span>
        <span class="auth__ray auth__ray--line"></span>
      </div>
      <span class="auth__wordmark">Prisma Games</span>
    </div>

    <div class="auth__form-side">
      <div class="auth__switch">
        <%= link_to t("auth.switch.sign_in"), new_session_path(resource_name) %>
        <%= link_to t("auth.switch.sign_up"), new_registration_path(resource_name), class: "is-active" %>
      </div>

      <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: "auth__form", novalidate: true }) do |f| %>
        <%= render "shared/form_errors", record: resource %>

        <div class="field">
          <%= f.label :full_name, t("auth.fields.full_name"), class: "field__label" %>
          <%= f.text_field :full_name, autofocus: true, autocomplete: "name", class: "field__input", required: true, placeholder: t("auth.placeholders.full_name") %>
        </div>

        <div class="field">
          <%= f.label :email, t("auth.fields.email"), class: "field__label" %>
          <%= f.email_field :email, autocomplete: "email", class: "field__input", required: true, placeholder: "voce@email.com" %>
        </div>

        <div class="auth__row">
          <div class="field">
            <%= f.label :cpf, t("auth.fields.cpf"), class: "field__label" %>
            <%= f.text_field :cpf, autocomplete: "off", class: "field__input", required: true, data: { "mask-cpf": true }, inputmode: "numeric", placeholder: "000.000.000-00" %>
          </div>
          <div class="field">
            <%= f.label :phone, t("account.profile.phone_label"), class: "field__label" %>
            <%= f.telephone_field :phone, autocomplete: "tel", class: "field__input", required: true, data: { "mask-phone": true }, placeholder: "(00) 00000-0000" %>
          </div>
        </div>

        <div class="auth__row">
          <div class="field">
            <%= f.label :password, t("auth.fields.password"), class: "field__label" %>
            <%= f.password_field :password, autocomplete: "new-password", class: "field__input", required: true, minlength: @minimum_password_length, placeholder: "••••••••" %>
            <% if @minimum_password_length %>
              <span class="field__hint"><%= t("devise.shared.minimum_password_length", count: @minimum_password_length) %></span>
            <% end %>
          </div>
          <div class="field">
            <%= f.label :password_confirmation, t("auth.fields.password_confirmation"), class: "field__label" %>
            <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "field__input", required: true, placeholder: "••••••••" %>
          </div>
        </div>

        <%= f.submit t("auth.sign_up.submit"), class: "button button--block auth__submit" %>

        <p class="auth__terms"><%= t("auth.sign_up.terms_html").html_safe %></p>
      <% end %>

      <div class="auth__foot">
        <%= t("auth.sign_up.has_account") %>
        <%= link_to t("auth.sign_up.sign_in"), new_session_path(resource_name) %>
      </div>
    </div>
  </div>
</div>
